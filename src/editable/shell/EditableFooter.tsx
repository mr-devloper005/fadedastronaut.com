'use client'
import Link from 'next/link'
import { SITE_CONFIG } from '@/lib/site-config'
import { useEditableLocalAuthSession } from '@/editable/components/EditableLocalAuthForms'
const links=[{label:'Classified',href:'/classified'},{label:'Home',href:'/'},{label:'About',href:'/about'},{label:'Search',href:'/search'}]
export function EditableFooter(){const{session,logout}=useEditableLocalAuthSession();return <footer className="bg-[#073032] text-white"><div className="mx-auto grid max-w-[1280px] gap-12 px-5 py-16 sm:px-8 lg:grid-cols-[1.3fr_.8fr_.8fr]"><div><Link href="/" className="editable-display inline-flex items-center gap-4 text-2xl font-extrabold"><img src="/favicon.png" alt="" className="h-20 w-20 shrink-0 object-contain" /><span>{SITE_CONFIG.name}</span></Link><p className="mt-5 max-w-md leading-7 text-white/65">A clear place to discover useful classifieds, services, products, rentals, jobs, and local offers.</p></div><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#42dfb2]">Explore</p><div className="mt-5 grid gap-3">{links.map(i=><Link key={i.href} href={i.href} className="text-sm font-semibold text-white/75 hover:text-white">{i.label}</Link>)}</div></div><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#42dfb2]">Account</p><div className="mt-5 grid gap-3">{session?<><Link href="/create" className="text-sm font-semibold text-white/75 hover:text-white">Create</Link><button onClick={logout} className="text-left text-sm font-semibold text-white/75 hover:text-white">Logout</button></>:<><Link href="/login" className="text-sm font-semibold text-white/75 hover:text-white">Login</Link><Link href="/signup" className="text-sm font-semibold text-white/75 hover:text-white">Sign up</Link></>}</div></div></div><div className="border-t border-white/10 px-5 py-5 text-center text-xs text-white/50">© {new Date().getFullYear()} {SITE_CONFIG.name}. All rights reserved.</div></footer>}
